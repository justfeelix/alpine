# dbt packages are installed with `dbt deps`, not pip — they are Jinja/SQL, not Python.
# They land in dbt_packages/ (gitignored) and are pinned here.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.1", "<2.0.0"]
